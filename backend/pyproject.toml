[project]
name = "echo-edger"
version = "0.1.0"
description = "Backend for Echo Ledger"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.129.0",
    "uvicorn>=0.41.0",
    "websockets>=16.0",
]
